apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tyk-app-claim
  namespace: tyk-oss
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard  # Adjust according to your GKE cluster's storage class
